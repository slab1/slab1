// Performance tracker (alias for performance-monitoring)
import { performanceMonitor } from './performance-monitoring';

export const performanceTracker = performanceMonitor;
export default performanceTracker;
